{% extends 'AdminBundle::layout.html.twig' %}

{% block currentItem %}taxonomy.{{ taxonomy.slug }}{% endblock %}
{% block content %}
    <div class="content--container">
        <div class="container--white">
            <div class="action-list">
                <a class="btn btn-default" href="{{ url('admin.taxonomy', {'id': taxonomy.id}) }}"><i class="fa fa-chevron-left"></i> Back</a>
            </div>
            <br/>
            {% include 'AdminBundle:TaxonomyItem:_form.html.twig' with {'isNew': true, 'taxonomy': taxonomy, 'taxonomyItem': taxonomyItem} %}
        </div>
    </div>
{% endblock %}
