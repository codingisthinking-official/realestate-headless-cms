{% extends 'AdminBundle::layout.html.twig' %}

{% block currentItem %}taxonomies{% endblock %}
{% block content %}
    <div class="content--container">
        <div class="container--white">
            <div class="action-list">
                <a class="btn btn-default" href="{{ url('admin.taxonomies.fields_category', {'id': taxonomy.id}) }}"><i class="fa fa-chevron-left"></i> Back</a>
            </div>
            <br/>
            {% include 'AdminBundle:TaxonomyCategoryFields:_form.html.twig' %}
        </div>
    </div>
{% endblock %}

